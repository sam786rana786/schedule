services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: fastapi-backend
    container_name: fastapi-backend
    restart: unless-stopped
    tty: true
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=mysql+pymysql://9eyJzdWIiOiJmY:IUzI1NiIsInR5cCI6I@mysql:3306/9oQG1haWxpbm
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    networks:
      - app-network
    depends_on:
      - mysql

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: vue-frontend
    container_name: vue-frontend
    restart: unless-stopped
    tty: true
    environment:
      - NODE_ENV=development
      - VITE_API_URL=http://localhost:8000
    volumes:
      - ./frontend:/app
    ports:
      - "8080:8080"
    networks:
      - app-network

  mysql:
    image: mysql:8.0
    container_name: mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: IUzI1NiIsInR5cCI6I
      MYSQL_DATABASE: 9oQG1haWxpbm
      MYSQL_USER: 9eyJzdWIiOiJmY
      MYSQL_PASSWORD: IUzI1NiIsInR5cCI6I
    volumes:
      - mysql-data:/var/lib/mysql
    ports:
      - "3306:3306"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  mysql-data: